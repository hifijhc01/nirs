#!/usr/bin/env bash
set -Eeuo pipefail
DEPLOY_DIR="${DEPLOY_DIR:-/deployments}"
SRC_DIR="${S2I_SOURCE_DIR:-/tmp/src}"
mkdir -p "${DEPLOY_DIR}"
cp -f "${SRC_DIR}/ROOT.war" "${DEPLOY_DIR}/ROOT.war"
chmod -R g+rwX "${DEPLOY_DIR}"
echo "[assemble] Deployed ROOT.war to ${DEPLOY_DIR}"

